version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3005:3005"

  frontend:
    build: ./frontend
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend